# Nixpkgs

## Requirements

* Nix package manager: [https://nixos.org/download/](https://nixos.org/download/)

## Setup

* Adjust any paths or options inside [`flake.nix`](./flake.nix).
* Use [`flake.sh`](./flake.sh) to manage the profile.
